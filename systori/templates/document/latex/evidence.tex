
{% load latex i18n l10n customformatting %}
\documentclass[10pt, oneside, landscape]{letter}
\usepackage[utf8x]{inputenc}

\usepackage{wallpaper, array, tabu, booktabs, eurosym}
% wallpaper = wallpaper pdf, array to stretch the table,
% tabu variable width columns for table
% booktabs beautify table
% eurosym for the € symbol

\usepackage{tikz}
% tikz for karo pattern generation
\newcommand{\karos}[2]{
  \begin{tikzpicture}
    \draw[step=0.5cm, color=lightgray] (0,0) grid (#1 cm, #2 cm);
  \end{tikzpicture}
}

\usepackage[defaultsans]{droidsans}
\renewcommand*\familydefault{\sfdefault} %% Only if the base font of the document is to be typewriter style
\usepackage[T1]{fontenc}
% droidsans font

\ULCornerWallPaper{1}{softronic2_landscape.pdf}

% general page setup
\usepackage[a4paper,top=20mm,bottom=34mm,left=11mm,right=11mm]{geometry}
\usepackage{fancyhdr}%custom header package
\pagestyle{fancy}%custom header
\fancyhf{}%clear all
\renewcommand{\headrulewidth}{0pt}%clear head line

%settings for longtable/tabu
\renewcommand*{\arraystretch}{1.5}
{\setlength{\tabcolsep}{0.5mm}

\usepackage{ragged2e}
\RaggedRight

\begin{document}
\sloppy
\begin{letter}

{% for job in jobs.all %}

 {% for taskgroup in job.taskgroups.all|exclude_taskgroups_with_zero_billable_total  %}
  {% for task in taskgroup.tasks.all|exclude_tasks_with_zero_complete %}
   {% for instance in task.taskinstances.all %}
    \begin{tabu} to \textwidth {l X[l] X[r] X[l] X[r] X[l]}
    \large {% filter bold%} {% trans 'Evidence Sheet' %} {% endfilter %} & & & & & {% trans 'Date' %}: {{doc.document_date|localize}} \\
    {% filter bold%} {% trans 'Project' %} {% endfilter %} & \multicolumn5{l}{ {{job.project}} \textbf{/} {{job.name|tex_escape|trim}} \textbf{/} {{taskgroup.name}} } \\
    {% filter bold%} {% trans 'Task' %} {% endfilter %} & {{task.code|trim}} & \textbf{Kurztext} & \multicolumn3{l}{ {{task.name|trim}} } \\
    {% filter bold%} {% trans 'P-Amount' %} {% endfilter %} & {{task.qty|ubrdecimal}} {{task.unit}} & {% filter bold%} {% trans 'Amount' %} {% endfilter %} & {{task.complete|ubrdecimal}} {{task.unit}} & {% filter bold%} {% trans 'UP' %} {% endfilter %} & {{task.unit_price|money|tex_escape}} \\
    \end{tabu}
    \vspace{5mm}
    \karos{27.5}{12}
    \newpage
   {% endfor %}
  {% endfor %}
 {% endfor %}
{% endfor %}

\end{letter}
\end{document}
